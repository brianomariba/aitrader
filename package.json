{
  "name": "ai-trader",
  "version": "1.0.0",
  "description": "DBot-style AI Trading Robot with advanced analytics",
  "scripts": {
    "build": "npm run install-deps",
    "build-frontend": "cd frontend && npm run build",
    "copy-build": "echo '✅ Next.js handled by server - no copy needed'",
    "install-deps": "npm install --prefix backend && npm install --prefix frontend",
    "start": "npm start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}